import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const OnboardListings = () => {
	return (
		<View>
			<Text>OnboardListings</Text>
		</View>
	);
}

const styles = StyleSheet.create({})

export default OnboardListings;
