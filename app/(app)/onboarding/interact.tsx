import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const OnboardInteract = () => {
	return (
		<View>
			<Text>OnboardInteract</Text>
		</View>
	);
}

const styles = StyleSheet.create({})

export default OnboardInteract;
