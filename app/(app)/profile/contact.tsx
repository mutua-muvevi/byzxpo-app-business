import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const ProfileContactUs = () => {
	return (
		<View>
			<Text>ProfileContactUs</Text>
		</View>
	);
}

const styles = StyleSheet.create({})

export default ProfileContactUs;
